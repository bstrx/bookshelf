{% extends "OpensoftBookshelfBundle::layout.html.twig" %}

{% block content -%}
    <h1>Contact edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            Обратно к списку
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
